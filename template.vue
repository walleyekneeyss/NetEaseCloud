<!-- vbase -->
<!-- service 发送请求的 store保存数据 views/pages试图层  cps组件 -->
<!-- 48-382处理数据  Promise.all(promises) -->


<!-- 给map遍历生成的组件Item的最后一个加样式   {banners.map((item, index) => {})}
  @1 <span className={classNames('item', { active: index === currentIndex })}></span>
  优 @2 &:last-child{    // 结构化伪类 动态样式
        width:228px
   }
-->